class Robot:

    def __init__(self, nom, cellule):
        self.nom = nom
        self.cellule = cellule

    def seDeplacer(self, typeDeDeplacement):
        if typeDeDeplacement == 'E':   #Le robot veut se déplacer à droite
            if self.cellule
        elif typeDeDeplacement == 'O':   #Le robot veut se déplacer à gauche
        elif typeDeDeplacement == 'N':   #Le robot veut se déplacer en haut
        elif typeDeDeplacement == 'S':    #Le robot veut se déplacer en bas